name: Targeted academic refactor implementation

on:
  push:
    branches:
      - academic-level-class-subscription-refactor
    paths:
      - .github/workflows/targeted-refactor-implementation.yml

permissions:
  contents: write

jobs:
  implement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: academic-level-class-subscription-refactor
          fetch-depth: 0

      - name: Repair staged script quoting
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('.github/scripts/targeted_refactor.py')
          lines = path.read_text().splitlines()
          index = next(i for i, line in enumerate(lines) if 'Create or update the one logical progression item' in line)
          lines[index:index + 2] = ["        '''Create or update the one logical progression item for a student/run.'''"]
          path.write_text('\n'.join(lines) + '\n')
          PY

      - name: Trace student placement and progression UI
        run: |
          grep -R -n -E "Academic progression|Your next class has been assigned|Next class assigned|placement" frontend/src backend/app | head -n 200 || true

      - name: Apply targeted refactor
        run: python .github/scripts/targeted_refactor.py

      - name: Syntax check modified Python
        run: python -m compileall -q backend/app backend/tests/test_academic_refactor_regressions.py

      - name: Commit branch-only implementation
        run: |
          rm .github/workflows/targeted-refactor-implementation.yml
          rm .github/scripts/targeted_refactor.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: harden academic lifecycle invariants and retries"
          git push origin HEAD:academic-level-class-subscription-refactor
